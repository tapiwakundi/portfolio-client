import React from 'react'
import { Typography } from '../../components'
import styles from './index.module.css'

export const Footer = () => {
    return <section className={styles.footer}>
        <Typography.Callout2>Footer</Typography.Callout2>
    </section>
}